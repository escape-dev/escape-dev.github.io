// Toggle the dropdown visibility based on the specific dropdown id
function toggleDropdown(dropdownId) {
  const dropdown = document.getElementById(dropdownId);

  dropdown.classList.toggle("show");
}
